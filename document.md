# Kubernetes روی Cluster / Namespace / App تمرین عملی مدیریت

**تاریخ:** ۲۶ مرداد ۱۴۰۵

## فهرست مطالب
1. مقدمه
2. مرحله اول API مربوط به Cluster
3. مرحله دوم API مربوط به Namespace
4. مرحله سوم: API مربوط به App
5. نکات پایانی

## ۱. مقدمه
این تمرین با هدف کار عملی روی Kubernetes طراحی شده است تا بتوانید روی کلاسترهایی که در هفته قبل ساخته‌اید چند نوع اپلیکیشن را مرحله به مرحله Deploy و مدیریت کنید.

هدف اصلی این است که در عمل بررسی کنید:
- هر درخواست از فرانت‌اند چگونه وارد یک بک‌اند می‌شود؟
- بک‌اند چه تصمیمی می‌گیرد؟
- چه اطلاعاتی باید در دیتابیس ذخیره شود؟
- چه اطلاعاتی نباید در response برگردد؟
- در صورت خطا بین Kubernetes و دیتابیس چه اتفاقی می‌افتد؟
- در شرایط concurrency چه رفتاری باید داشته باشیم؟
- چه مواردی از نظر امنیتی اهمیت دارند؟

## ۲ مرحله اول API مربوط به Cluster
در این مرحله باید یک CRUD ساده برای Cluster ها طراحی کنید؛ اما فعلاً فقط دو متد مورد نیاز هستند:
- `POST`
- `GET`

### ۲.۱ ایجاد Cluster - متد POST
متد POST باید اطلاعات Cluster را دریافت کرده و فقط در دیتابیس ذخیره کند.
این متد نباید هیچ کار دیگری انجام دهد؛ از جمله:
- اتصال به Kubernetes
- ساخت Resource جديد
- بررسی وضعیت Cluster
- اجرای عملیات اضافی روی کلاستر

**نمونه Request:**
```json
{
  "name": "",
  "address": "95.43.54.43:6443",
  "token": ""
}
```

**نکته امنیتی:**
token یک اطلاعات حساس است. در صورتی که این Token لو برود ممکن است دسترسی مدیریتی به Cluster در اختیار فرد دیگری قرار بگیرد.
در این تمرین لازم نیست مکانیزم امنیتی برای Token پیاده سازی کنید؛ اما باید درباره روش‌های بهتر مدیریت اطلاعات حساس فکر کنید برای مثال:
- Encryption در زمان ذخیره سازی
- Secret Management
- محدود کردن دسترسی به Token
- عدم Log کردن Token
- عدم برگرداندن Token در API Response

### ۲.۲ دریافت لیست Cluster ها - متد GET
متد GET باید لیست Cluster ها را از دیتابیس دریافت کرده و به کاربر برگرداند.
از آنجا که در حال حاضر فقط یک Cluster داریم خروجی می‌تواند شامل یک Object باشد.

**نمونه Response:**
```json
[
  {
    "name": "cluster-1",
    "address": "95.43.54.43:6443"
  }
]
```
**نکته مهم:**
در Response نباید token برگردانده شود.

## ۳ مرحله دوم API مربوط به Namespace
در این مرحله باید CRUD مربوط به Namespaceها را پیاده سازی کنید.
متدهای مورد نیاز:
- `POST`
- `GET`
- `DELETE`

در Kubernetes برای جداسازی Resourceها داخل یک Cluster استفاده می‌شود.

### ۳.۱ ایجاد Namespace - متد POST
باید اطلاعات Request نام Cluster و Namespace را دریافت کند.

**نمونه Request:**
```json
{
  "cluster_id": 5,
  "name": "new-ns"
}
```

پس از دریافت Request Backend باید:
1. Cluster مربوطه را از دیتابیس پیدا کند.
2. به Kubernetes همان Cluster متصل شود.
3. Namespace مورد نظر را در Kubernetes ایجاد کند.
4. در صورت موفقیت اطلاعات Namespace را در دیتابیس Backend ذخیره کند.

از نظر Kubernetes این عملیات مشابه ایجاد Namespace با دستور زیر است:
`kubectl create namespace new-ns`

**تکراری Namespace**
اگر Namespace مورد نظر از قبل در Kubernetes وجود داشته باشد باید Error مناسب برگردانده شود.
همچنین باید مشخص باشد که هر خطا مربوط به چه چیزی است؛ برای مثال:
- Namespace از قبل وجود دارد.
- Cluster پیدا نشد.
- Backend مجوز ساخت Namespace ندارد.
- Kubernetes در دسترس نیست.

### ۳.۲ Response Code ها
در طراحی API به Response Codeهای مناسب فکر کنید.
بسته به شرایط می‌توان از کدهایی مانند موارد زیر استفاده کرد:

| Response Code | وضعیت |
| :--- | :--- |
| 200 OK | درخواست موفق |
| 201 Created | Resource جدید ایجاد شد |
| 400 Bad Request | ورودی نامعتبر |
| 401 Unauthorized | احراز هویت ناموفق |
| 403 Forbidden | عدم دسترسی |
| 404 Not Found | Resource پیدا نشد |
| 409 Conflict | مثلاً Conflict، Namespace تکراری |
| 500 Internal Server Error | خطای داخلی Backend |
| 502 Bad Gateway | خطا در ارتباط با Kubernetes |

لازم نیست همه این کدها را در هر API استفاده کنید؛ مهم این است که برای هر وضعیت Response مناسبی طراحی شود.

### ۳.۳ Throttling
به این موضوع نیز فکر کنید که یک کاربر نتواند در مدت کوتاهی تعداد بسیار زیادی Namespace ایجاد کند.
برای مثال: اگر یک کاربر در چند ثانیه صدها Request برای ساخت Namespace ارسال کند چه اتفاقی باید بیفتد؟
در این شرایط می‌توان از مفاهیمی مانند:
- Rate Limiting
- Throttling
- Request Quota

استفاده کرد. پیاده سازی کامل آن برای این تمرین الزامی نیست اما باید در طراحی به آن توجه داشته باشید.

### ۳.۴ دریافت Namespaceها - متد GET
متد GET باید `cluster_id` را به صورت Query Parameter دریافت کند.

**نمونه Request:**
`GET /namespace?cluster_id=5`

**نمونه Response:**
```json
[
  {
    "id": 3,
    "name": "new-ns"
  }
]
```

**نکته مهم** این است که Response فقط Namespace هایی را نشان دهد که Backend آن‌ها را ساخته و در دیتابیس ثبت کرده است.
بنابراین در این بخش Source of Truth = Database
یعنی اگر شخص دیگری مستقیماً یک Namespace در Kubernetes ساخته باشد Backend نباید صرفاً به خاطر وجود آن Namespace در Kubernetes آن را در لیست خود نمایش دهد.

### ۳.۵ حذف Namespace - متد DELETE
متد DELETE باید id مربوط به Namespace را دریافت کند.
سپس باید Namespace را:
1. از Kubernetes حذف کند.
2. از دیتابیس Backend حذف کند.

**مثال:**
`DELETE /namespace/3`

### ۳.۶ Edge Case های حذف Namespace
در این بخش باید به شرایط خطا و عملیات هم زمان توجه کنید.

**حالت اول: حذف از Kubernetes موفق، حذف از Database ناموفق**
فرض کنید Backend:
1. Namespace را از Kubernetes حذف می‌کند.
2. قبل از حذف رکورد از دیتابیس Backend Crash می‌کند.

در این شرایط چه اتفاقی می‌افتد؟ Database هنوز فکر می‌کند Namespace وجود دارد در حالی که در Kubernetes دیگر وجود ندارد.
باید درباره راهکارهایی مانند موارد زیر فکر کنید:
- Retry
- Reconciliation
- وضعیت‌های میانی در Database
- Job یا Queue برای عملیات غیر همزمان
- Periodic Sync

**حالت دوم: دو درخواست هم زمان برای حذف**
فرض کنید دو کاربر همزمان درخواست زیر را ارسال کنند:
`DELETE /namespace/3`
ممکن است هر دو Request تقریباً هم زمان اجرا شوند.
باید مشخص کنید:
- کدام Request موفق شود؟
- Request دوم چه Response Code ی دریافت کند؟
- آیا باید 404 Not Found برگردد؟
- آیا 409 Conflict مناسب تر است؟
- چگونه از Race Condition جلوگیری می‌کنید؟

هدف این بخش توجه به Concurrency و Consistency است.

## ۴ مرحله سوم: API مربوط به App
در این مرحله باید یک CRUD برای Appها طراحی کنید.
متدهای مورد نیاز:
- `POST`
- `GET`
- `UPDATE`
- `DELETE`

در این قسمت مثال دقیق Request و Response داده نمی‌شود و انتظار می‌رود خودتان API را طراحی کنید. مهمتر از پیاده سازی پیچیده این است که نیازمندی‌ها را به درستی درک کنید و API تمیز و منطقی طراحی کنید.

### ۴.۱ ایجاد App - متد POST
متد POST باید اطلاعات مورد نیاز برای ساخت یک App را دریافت کند. Backend باید بر اساس این اطلاعات یک Deployment در Namespace مورد نظر ایجاد کند.
برای مثال اطلاعاتی مانند موارد زیر احتمالاً مورد نیاز هستند:
- نام App
- Namespace
- Image
- تعداد Replicaها
- CPU
- Memory
- سایر تنظیمات مورد نیاز

طراحی دقیق Request بر عهده شماست.

### ۴.۲ دریافت Appها - متد GET
متد GET باید لیست Appهای یک Namespace مشخص را برگرداند.
اطلاعاتی مانند موارد زیر می‌توانند در Response وجود داشته باشند:
```json
[
  {
    "id": 1,
    "name": "my-app",
    "namespace": "new-ns",
    "replicas": 2,
    "ready": true
  }
]
```
ساختار دقیق Response بر عهده شماست.

### ۴.۳ وضعیت App و Podها
یکی از نکات مهم این مرحله این است که وضعیت Pod ها باید Live از Kubernetes خوانده شود.
یعنی وضعیت فعلی Podها را صرفاً از Database نخوانید.
برای مثال اگر Deployment دارای سه Pod باشد:
- Pod 1: Ready
- Pod 2: Ready
- Pod 3: Not Ready

اینکه به چه صورت می‌خواهید این وضعیت‌ها را در ریسپانس مربوط به GET برگردانید بر عهده شماست. می‌خواهید وضعیت هر پاد را جدا نشان دهید؟ می‌خواهید به اگر یکی هم Not Ready بود وضعیت را Not Ready نشان دهید؟ تصمیم گیری بر عهده شماست.

### ۴.۴ بروزرسانی App - متد UPDATE
متد UPDATE باید امکان تغییر تنظیمات App را فراهم کند.
برای مثال:
`CPU: 500m → 1000m`
یا
`Memory: 512Mi → 1Gi`
همچنین ممکن است مواردی مانند تعداد Replicaها نیز قابل تغییر باشند.
Backend باید تغییرات مورد نظر را در Kubernetes اعمال کند و وضعیت مورد نیاز Database را نیز مدیریت کند.

### ۴.۵ حذف App - متد DELETE
متد DELETE باید App را هم از:
1. Kubernetes
2. Database
حذف کند.

مشابه Namespace در اینجا نیز باید به سناریوهایی مانند موارد زیر فکر کنید:
- Kubernetes موفق شود ولی Database خطا بدهد.
- Database موفق شود ولی Kubernetes خطا بدهد.
- App از قبل در Kubernetes حذف شده باشد.
- دو درخواست همزمان برای حذف App ارسال شود.
- Backend هنگام عملیات Crash کند.
